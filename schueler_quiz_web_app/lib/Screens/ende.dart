import 'package:flutter/material.dart';

class Ende extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Die Zeit ist abgelaufen vielen Dank für die Teilname! Du hast XY von Z Punkten erreicht!'),
    );
  }
}